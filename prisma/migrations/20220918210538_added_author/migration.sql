BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Article] ADD [author] VARCHAR(255) CONSTRAINT [Article_author_df] DEFAULT 'NoName';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
